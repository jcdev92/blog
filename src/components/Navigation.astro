---
const pageTitles = [
    {title: "Home", href: "/"},
    {title: "About", href: "/about/"},
    {title: "Blog", href: "/blog/"},
]
---
<nav>
    {pageTitles.map((page) => (
        <a href={page.href}>{page.title}</a>
    ))}
</nav>